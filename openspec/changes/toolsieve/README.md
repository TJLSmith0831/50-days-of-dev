# toolsieve

Standalone open-source MCP server that aggregates real downstream MCP servers and routes tool selection via semantic embedding matching, proxies calls through, and reports live token savings vs. naive all-tools binding. Ships as its own new GitHub repo plus a Claude Code plugin wrapper.
