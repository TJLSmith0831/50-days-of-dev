[project]
name = "day-10-subagent-mcp"
version = "0.0.0"
requires-python = ">=3.13"
dependencies = [
  "mcp>=1.0",
  "ollama>=0.4",
  "ddgs>=9.0",
]

[dependency-groups]
dev = [
  "pytest>=8.0",
  "pytest-asyncio>=0.24",
]

[tool.uv]
package = false
