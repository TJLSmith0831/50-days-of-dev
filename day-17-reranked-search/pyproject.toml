[project]
name = "day-17-reranked-search"
version = "0.0.0"
requires-python = ">=3.13,<3.14"
dependencies = [
  "fastembed",
  "datasets",
  "numpy",
]

[dependency-groups]
dev = [
  "pytest",
]

[tool.uv]
package = false

[tool.pytest.ini_options]
pythonpath = ["."]
