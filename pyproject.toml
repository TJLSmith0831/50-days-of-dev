[project]
name = "fifty-days-of-dev"
version = "0.0.0"
requires-python = ">=3.13"

[tool.uv.workspace]
members = ["day-*"]
exclude = ["day-07-armadillo-cli", "day-08-docs-mcp", "day-09-cached-weather-mcp"]

[tool.uv]
package = false
