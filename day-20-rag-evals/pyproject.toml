[project]
name = "day-20-rag-evals"
version = "0.0.0"
requires-python = ">=3.13,<3.14"
dependencies = [
  "huggingface-hub",
  "openai",
  "pydantic",
  "python-dotenv",
  "rich",
]

[tool.uv]
package = false

[tool.ruff.lint.isort]
known-first-party = ["agent", "corpus", "evals"]
